name: AgentSec CI

on:
  push:
    branches: [main, agent-skill-v1]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Python syntax
        run: |
          python -m py_compile scripts/agentsec.py scripts/architecture_inventory.py

      - name: Shell syntax
        run: |
          bash -n agentsec
          bash -n install-deps.sh
          bash -n scripts/local_server_audit.sh

      - name: Unit tests
        run: python -m unittest discover -s tests -v

      - name: CLI smoke test
        run: |
          python scripts/agentsec.py --help
          python scripts/architecture_inventory.py . --output /tmp/agentsec-architecture.json
          python - <<'PY'
          import json
          with open('/tmp/agentsec-architecture.json', encoding='utf-8') as f:
              data = json.load(f)
          assert data['tool'] == 'AgentSec architecture inventory'
          assert 'signals' in data
          assert 'opportunities' in data
          print('Architecture inventory smoke test passed')
          PY

      - name: Validate Agent Skills specification
        run: |
          python -m pip install --upgrade pip
          python -m pip install skills-ref
          rm -rf /tmp/agentsec
          mkdir -p /tmp/agentsec
          cp SKILL.md /tmp/agentsec/
          cp -r scripts references /tmp/agentsec/
          skills-ref validate /tmp/agentsec
